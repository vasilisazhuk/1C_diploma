@Обработчик
метод ПередЗаписью()
    ОбновитьРеквизиты(Объект)
;

@ВТипе
@НаСервере @ДоступноСКлиента
статический метод ОбновитьРеквизиты(О: УставнойКапитал.Объект)
    О.Автор  = Пользователи.ТекущийПользователь
    О.ДатаАктуализации = Момент.Сейчас().ВДатаВремя(ЧасовойПояс.Текущий())
;